# OAuth Service Configuration
# This service handles OAuth authentication for Google, GitHub, and Slack

# Server Configuration
NODE_ENV=development
PORT=3002
LOG_LEVEL=info

# Security (REQUIRED)
# Generate with: openssl rand -hex 32
COOKIE_SECRET=your-32-character-cookie-secret-here!!!!!!!!
JWT_SECRET=your-32-character-jwt-secret-here!!!!!!!!!!

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# OAuth Providers (At least one provider must be configured)
# Google OAuth (https://console.cloud.google.com/)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3002/oauth/google/callback

# GitHub OAuth (https://github.com/settings/developers)
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret
# GITHUB_REDIRECT_URI=http://localhost:3002/oauth/github/callback

# Slack OAuth (https://api.slack.com/apps)
# SLACK_CLIENT_ID=your-slack-client-id
# SLACK_CLIENT_SECRET=your-slack-client-secret
# SLACK_REDIRECT_URI=http://localhost:3002/oauth/slack/callback

# Frontend Configuration
FRONTEND_URL=http://localhost:5173

# MCP Gateway URL
MCP_BASE_URL=http://localhost:3001

# Session Configuration
SESSION_DURATION_DAYS=30

# Admin API Key (Optional - for admin endpoints)
# Generate with: openssl rand -hex 32
# ADMIN_KEY=your-32-character-admin-key-here!!!!!!!!

# Database and Encryption Key are inherited from root .env