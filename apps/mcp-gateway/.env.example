# MCP Gateway Configuration
# This service routes MCP requests to appropriate servers

# Server Configuration
NODE_ENV=development
PORT=3001
HOST=localhost

# Database and Encryption Key are inherited from root .env