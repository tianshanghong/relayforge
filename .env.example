# RelayForge Local Development Environment
# Copy this file to .env for local development
# For production, use .env.production.example
# For development server, use .env.development.example

# Environment
NODE_ENV=development

# Database Configuration (for local development)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/relayforge_local?schema=public

# Encryption key for OAuth tokens (REQUIRED - 32 bytes hex)
# Generate with: pnpm run generate-keys
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Internal API key for service-to-service communication (REQUIRED - 32 bytes hex)
# Generate with: openssl rand -hex 32
INTERNAL_API_KEY=

# Security Keys (generate with: pnpm run generate-env-keys)
JWT_SECRET=
COOKIE_SECRET=
ADMIN_KEY=

# Google OAuth (for local development)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Local Domain Configuration
DOMAIN_NAME=localhost
FRONTEND_URL=http://localhost:5173
OAUTH_SERVICE_URL=http://localhost:3002
MCP_BASE_URL=http://localhost:3001
GOOGLE_REDIRECT_URI=http://localhost:3002/oauth/google/callback

# Frontend build variables
VITE_API_BASE_URL=http://localhost:3002
VITE_OAUTH_SERVICE_URL=http://localhost:3002